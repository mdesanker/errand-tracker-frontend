import styled from "styled-components";

const RequestPending = () => {
  return <Wrapper>Pending...</Wrapper>;
};

const Wrapper = styled.p`
  font-size: 0.9rem;
  padding: 2px 5px;
  color: gray;
`;

export default RequestPending;
